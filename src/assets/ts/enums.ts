enum Role {
  admin = "admin",
  researcher = "researcher",
  viewer = "viewer",
}

enum Variant {
  default = "default",
  link = "link",
  destructive = "destructive",
  outline = "outline",
  secondary = "secondary",
  ghost = "ghost",
}

export { Role, Variant };
